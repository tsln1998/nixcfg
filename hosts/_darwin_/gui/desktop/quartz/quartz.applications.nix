{
  system.defaults = {
    ActivityMonitor = {
      # 显示全部进程；100 对应 Activity Monitor 的“All Processes”。
      ShowCategory = 100;
      # 启动活动监视器时不自动打开主窗口。
      OpenMainWindow = false;
    };

    iCal = {
      # 默认隐藏日历应用左侧的日历列表。
      CalendarSidebarShown = false;
    };
  };
}
